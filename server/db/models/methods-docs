
//regular models
Order
  Order.createNewOrder() //this creates a new order and simultaneously deletes all the items
from the user's cart
  Order.getOrdersByUser()
  Order.getOrdersBySession()
Product
User
  instance methods
    User.getProducts() //this will get their cart!
    User.addProduct() //add to their cart
    User.deleteCart() //deletes all items from cart
Guest


//Join tables
Cart
LineItem
SessionCart
